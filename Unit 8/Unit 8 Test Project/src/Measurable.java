// Dominic Rutkowski
//
/* The Measurable interface allows objects
   that have a measurable attribute to be
   treated like one another in methods that
   only need a measurable piece of data.
*/

public interface Measurable
{
	String getResult();
}